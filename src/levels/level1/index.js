import Commands from './commands';
import Env from './env';
import FS from './fs';
import History from './history';
import Output from './output';
import Data from './data';

const Level = {
    Commands, Env, FS, History, Output, Data
}

export default Level;